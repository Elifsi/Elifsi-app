name: CI

on:
  push:
    branches: [main]
    paths:
      - "prototype/Phone/**"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "prototype/Phone/**"
      - ".github/workflows/ci.yml"

defaults:
  run:
    working-directory: prototype/Phone

jobs:
  prototype:
    name: Prototype — lint / typecheck / test / build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: prototype/Phone/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Unit + integration tests
        run: npm run test

      - name: Production build
        run: npm run build

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: End-to-end tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: prototype/Phone/playwright-report/
          retention-days: 7
